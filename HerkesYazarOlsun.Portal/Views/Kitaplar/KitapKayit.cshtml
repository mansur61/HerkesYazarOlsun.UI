@using HerkesYazarOlsun.Portal.Controllers;
@{
    ViewData["Title"] = "Kitap Ekle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/lib/bootstrap/dist/css/kitapekle.css" rel="stylesheet" />
<br />
<br />

<!-- CONTAINER -->
<div class="container d-flex align-items-center min-vh-100">
    <br />
    <div class="row g-0 justify-content-center">
        <!-- TITLE -->
        <div class="col-lg-4 offset-lg-1 mx-0 px-0">
            <div id="title-container">
                @*<img class="covid-image" src="./img/covid-check.png">*@
                <h2>Herkes Yazar Olsun</h2>
                @*<h3>Herkes Yazar Olsun Form</h3>*@
                <br />
                <p>
                    Siz değerli yazarımız.
                    İçeriğinizi tüm paydaşlarımızla paylaşacağınızdan ötürü mutluluk içirisindeyiz.
                    Bu yolda başarılarınızı temenni ederiz.
                </p>
            </div>
        </div>
        <!-- FORMS -->
        <div class="col-lg-7 mx-0 px-0">
            <div class="progress">
                <div aria-valuemax="100" aria-valuemin="0" aria-valuenow="50" class="progress-bar progress-bar-striped progress-bar-animated bg-danger" role="progressbar" style="width: 0%"></div>
            </div>
            <div id="qbox-container">
                @*<form class="needs-validation" id="form-wrapper" method="post" name="form-wrapper" novalidate="">*@
                <div id="steps-container">

                    <div class="step">
                        <h1><strong>Kitabınızı Yazmaya Başlayınız </strong> </h1>
                        <h4>Kitabınız İçin Önbilgileri Formlardan Doldurunuz ... </h4>
                        <br />
                        <div class="conrainer">

                            <div class="mb-3">
                                <h5>Kitabınızın Adını Belirleyiniz</h5>
                                <input class="form-control" type="text" id="kitapAdi" multiple>

                            </div>

                            <div class="mb-3">
                                <h5>Kitabınızın Ön Kapağını Seçiniz</h5>
                                <input class="form-control" type="file" id="formFileMultipleOnkapak" multiple>

                            </div>
                            <div class="mb-3">
                                <h5>Kitabınızın Arka Kapağını Seçiniz</h5>
                                <input class="form-control" type="file" id="formFileMultipleArkakapak" multiple>
                            </div>
                            <div class="mb-3">
                                <h5>Kitabınızın Önsöz'ünü giriniz</h5>
                                <textarea class="form-control" id="exampleFormControlTextarea1" rows="5" cols="50"></textarea>
                                <div id="help" class="form-text" style="color:cadetblue">500 karakteri geçmeyiniz.</div>
                            </div>

                            <div class="mb-3">
                                <h5>Kitabınızın Arkakapak yazısını giriniz</h5>
                                <textarea class="form-control" id="exampleFormControlTextarea1" rows="5" cols="50"></textarea>
                                <div id="help" class="form-text" style="color:cadetblue">200 karakteri geçmeyiniz.</div>
                            </div>
                            <h5>Kitabın Kategorisini seçiniz</h5>
                            <div class="row-cols-3">
                            </div>

                            <select class="form-select" style="width:500px;height:30px">
                                <option selected>Köşe Yazarı</option>
                                <option value="1">Hikaye</option>
                                <option value="2">Masal</option>
                                <option value="3">Şiir</option>
                                <option value="3">Roman</option>
                            </select>
                        </div>
                        @* <div class="form-check ps-0 q-box" style="border:1px solid red;">
                        <div class="q-box__question">
                        <input class="form-check-input question__input" id="q_1_yes" name="q_1" type="radio" value="Yes">
                        <label class="form-check-label question__label" for="q_1_yes">Yes</label>
                        </div>
                        <div class="q-box__question" >

                        <input checked class="form-check-input question__input" id="q_1_no" name="q_1" type="radio" value="No">
                        <label class="form-check-label question__label" for="q_1_no">No</label>
                        </div>
                        </div>*@
                    </div>

                    <div class="step">
                        <h1><strong>Kitabınızın Sayfalarını Oluşturunuz.</strong> </h1>
                        <p id="sayfaSayisi">1</p>
                        <br />
                        <div class="conrainer">
                            <div class="mb-3">
                                <h5>İlgili sayfaya resim koyabilirsiniz</h5>
                                <input class="form-control" type="file" id="formFileMultipleIlgiliSayfa" multiple>

                            </div>

                            <div class="mb-3">
                                <h5>İlgili Sayfayı yazınız</h5>
                                <div id="help" class="form-text" style="color:cadetblue">1000 karakteri geçmeyiniz.</div>
                                <textarea class="form-control" id="exampleFormControlTextarea1" rows="30" cols="50"></textarea>

                            </div>

                        </div>
                    </div>


                </div>
                <div id="q-box__buttons">
                    <button id="prev-btn" type="button">Geri</button>
                    <button id="next-btn" type="button">İleri</button>
                    <button id="submit-btn" type="submit" onclick="Kaydet()">Kaydet</button>
                </div>
                @*</form>*@
            </div>
        </div>
    </div>
</div>

<div id="preloader-wrapper">
    <div id="preloader"></div>
    <div class="preloader-section section-left"></div>
    <div class="preloader-section section-right"></div>
</div>
<br />
<br />

@*<script type="text/javascript" src="js/kitapekle.js"></script>*@

<script>

    let step = document.getElementsByClassName('step');
    let prevBtn = document.getElementById('prev-btn');
    let nextBtn = document.getElementById('next-btn');
    let submitBtn = document.getElementById('submit-btn');
    let form = document.getElementsByTagName('form')[0];
    let preloader = document.getElementById('preloader-wrapper');
    let bodyElement = document.querySelector('body');


    //form.onsubmit = () => {
    //    return false
    //}

    var current_step = 0;
    var stepCount = step.length;
    step[current_step].classList.add('d-block');
    step[current_step].style.display = "block";
    if (current_step == 0) {
        prevBtn.classList.add('d-none');
        submitBtn.classList.add('d-none');
        nextBtn.classList.add('d-inline-block');
    }
    const progress = (value) => {
        document.getElementsByClassName('progress-bar')[0].style.width = `${value}%`;
    }
    var previous_step = 0;

    nextBtn.addEventListener('click', () => {
        current_step++;
        console.log("nextBtn current_step:" + current_step);
        var previous_step = current_step - 1;
        console.log("previous_step:" + previous_step);

        if ((current_step > 0) && (current_step < stepCount)) {

            prevBtn.classList.remove('d-none');
            prevBtn.classList.add('d-inline-block');


            step[current_step].classList.remove('d-none');
            step[current_step].style.display = "none";

            step[current_step].classList.add('d-block');
            step[current_step].style.display = "block";

            step[previous_step].classList.remove('d-block');
            step[previous_step].style.display = "block";

            step[previous_step].classList.add('d-none');
            step[previous_step].style.display = "none";

            if (current_step == stepCount - 1) { // 3 tane eleman var ise 3. index olmaz son eleman 2. indextir

                submitBtn.classList.remove('d-none');
                submitBtn.classList.add('d-inline-block');

                nextBtn.classList.remove('d-inline-block');
                nextBtn.classList.add('d-none');
            }

        } else {

            if (current_step > stepCount) {
                form.onsubmit = () => {
                    return true
                }
            }
        }
        progress((100 / stepCount) * current_step);
    });


    prevBtn.addEventListener('click', () => {
        if (current_step > 0) {
            console.log("prevBtn current_step:" + current_step);
            current_step--;
            let previous_step = current_step + 1;


            console.log("previous_step:" + previous_step);

            prevBtn.classList.add('d-none');
            prevBtn.classList.add('d-inline-block');


            step[current_step].classList.remove('d-none');
            step[current_step].style.display = "none";

            step[current_step].classList.add('d-block')
            step[current_step].style.display = "block";
            if (previous_step < stepCount) {
                step[previous_step].classList.remove('d-block');
                step[previous_step].style.display = "block";

                step[previous_step].classList.add('d-none');
                step[previous_step].style.display = "none";
            }


            if (current_step < stepCount) {
                submitBtn.classList.remove('d-inline-block');
                submitBtn.classList.add('d-none');
                nextBtn.classList.remove('d-none');
                nextBtn.classList.add('d-inline-block');
                prevBtn.classList.remove('d-none');
                prevBtn.classList.add('d-inline-block');
            }
        }

        if (current_step == 0) {
            prevBtn.classList.remove('d-inline-block');
            prevBtn.classList.add('d-none');
        }
        progress((100 / stepCount) * current_step);
    });


    submitBtn.addEventListener('click', () => {
        preloader.classList.add('d-block');

        const timer = ms => new Promise(res => setTimeout(res, ms));

        timer(3000)
            .then(() => {
                bodyElement.classList.add('loaded');
            }).then(() => {

                console.log("geldi.. submit buton:" + stepCount + " step sayısı :" + step.length);
                if (step.length < stepCount) {
                    step[stepCount].classList.remove('d-block');
                    step[stepCount].style.display = "block";

                    step[stepCount].classList.add('d-none');
                    step[stepCount].style.display = "none";
                }


                prevBtn.classList.remove('d-inline-block');
                //prevBtn.classList.add('d-none');
                submitBtn.classList.remove('d-inline-block');
                //submitBtn.classList.add('d-none');

            })

    });


    function Kaydet() {
        $.get("@Url.Action(nameof(KitaplarController.KitapEkle), "Kitaplar")", {}, function (data) {
            console.log("data: " + data);
            if(data.state == 1){
                $("#sayfaSayisi").html(data);
            }
        });

    }

</script>

