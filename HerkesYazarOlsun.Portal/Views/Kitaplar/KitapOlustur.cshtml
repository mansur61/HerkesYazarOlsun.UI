@using HerkesYazarOlsun.Portal.Controllers;
@model HerkesYazarOlsun.Model.ViewModel.VM_BOOKS_DETAIL;

@{
    ViewData["Title"] = "Kitap Ekle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {

    <link href="~/lib/bootstrap/dist/css/kitapekle.css" rel="stylesheet" />
    <style>
        body {
            background: #f7f9ff;
            font-family: 'Josefin Sans', sans-serif;
            font-size: 15px;
            font-weight: 400;
            line-height: 1.4;
            color: #555;
        }

        button#tamam-btn {
            font-size: 17px;
            font-weight: bold;
            position: relative;
            width: 130px;
            height: 50px;
            background: #DC3545;
            margin: 0 auto;
            margin-top: 40px;
            overflow: hidden;
            z-index: 1;
            cursor: pointer;
            transition: color .3s;
            text-align: center;
            color: #fff;
            border: 0;
            -webkit-border-bottom-right-radius: 5px;
            -webkit-border-bottom-left-radius: 5px;
            -moz-border-radius-bottomright: 5px;
            -moz-border-radius-bottomleft: 5px;
            border-bottom-right-radius: 5px;
            border-bottom-left-radius: 5px;
        }
    </style>
}

<br />
<br />

@{
    string tip = "kitapekle";
}
<input type="hidden" id="tip" value="@tip" />


<!-- CONTAINER -->
<div class="container d-flex align-items-center min-vh-100">
    <br />
    <div class="row g-0 justify-content-center">

        <!-- TITLE -->
        <div class="col-lg-4 offset-lg-1 mx-0 px-0">
            <div id="title-container">
                <h2>Herkes Yazar Olsun</h2>
                <br />
                <p>
                    Siz değerli yazarımız.
                    İçeriğinizi tüm paydaşlarımızla paylaşacağınızdan ötürü mutluluk içirisindeyiz.
                    Bu yolda başarılarınızı temenni ederiz.
                </p>
            </div>
            <br />
            @Html.Partial("_AlertMesaj", @tip)
        </div>
        <!-- FORMS -->
        <div class="col-lg-7 mx-0 px-0">
            <div class="progress">
                <div aria-valuemax="100" aria-valuemin="0" aria-valuenow="50" class="progress-bar progress-bar-striped progress-bar-animated bg-danger" role="progressbar" style="width: 0%"></div>
            </div>

            <div id="qbox-container">
                <form action="KitapEkle" method="post" enctype="multipart/form-data" class="needs-validation" id="form-wrapper">

                    <div id="steps-container">

                        <div class="step">
                            @Html.Partial("_KitapEkleOnBilgiler", @Model)

                        </div>

                        <div class="step">
                            <h1><strong>Kitabınızın Sayfalarını Oluşturunuz.</strong> </h1>
                            <p>
                                Kitabta Yazılan İlgili Sayfa No :
                                @if (@ViewBag.BOOK_ID.HasValue && @ViewBag.BOOK_ID != 0)
                                {
                                    <span id="sayfaSayisi" data-value="@Model.IlgiiSayfaSayisi"> @Model.IlgiiSayfaSayisi </span>
                                }
                                else
                                {
                                    <span id="sayfaSayisi" data-value="@Model.IlgiiSayfaSayisi"> 1 </span>
                                }

                            </p>

                            <input class="form-control" type="hidden" id="kitapKatdetmeUyariDurumu" value="0">

                            <input class="form-control" type="hidden" id="BOOKSID" value="@ViewBag.BOOK_ID">
                            <input class="form-control" type="hidden" id="BOOKPAGESID" value="0">
                            <br />
                            <div class="conrainer">

                                <div class="mb-3">
                                    <h5>İlgili Sayfayı yazınız</h5>
                                    <div id="help" class="form-text" style="color:cadetblue" name="SAYFAYAZI">1000 karakteri geçmeyiniz.</div>
                                    <textarea class="form-control" id="sayfaYazi" rows="30" cols="50"></textarea>

                                </div>

                            </div>
                        </div>


                    </div>
                    <div id="q-box__buttons">
                        <button id="prev-btn" type="button">Geri</button>
                        <button id="next-btn" type="button">İleri</button>
                        <button id="submit-btn" type="submit">Kaydet</button>
                        <button id="tamam-btn" type="button">Tamamla</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<div id="preloader-wrapper">
    <div id="preloader"></div>
    <div class="preloader-section section-left"></div>
    <div class="preloader-section section-right"></div>
</div>
<br />
<br />

<div id="alertGoster"></div>

<script src="~/js/general.js"></script>

@section Scripts {
    <!-- SweetAlert2 JS dosyası ve Bootstrap JS dosyası -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>


}

<script>


    let step = document.getElementsByClassName('step');
    let prevBtn = document.getElementById('prev-btn');
    let tamamBtn = document.getElementById('tamam-btn');
    let nextBtn = document.getElementById('next-btn');
    let submitBtn = document.getElementById('submit-btn');
    let form = document.getElementsByTagName('form')[0];
    let preloader = document.getElementById('preloader-wrapper');
    let bodyElement = document.querySelector('body');

    var tip = $("#tip").val();

    const progress = (value) => {
        document.getElementsByClassName('progress-bar')[0].style.width = `${value}%`;
    }
    var spanElement = document.getElementById('sayfaSayisi');
    // data özelliğinden değeri al
    var dataValue = spanElement.getAttribute('data-value');

     console.log("dataValue : " + dataValue);


    $("#tamam-btn").on("click", function () {
        var kategoriBilgisi = $("#kitapKetogri").val();
        console.log("kategoriBilgisi : " + kategoriBilgisi);
        var kitapId = $("#BOOKSID").val();
        var sayfaYazi = $("#sayfaYazi").val();

        Swal.fire({
            title: 'Emin misiniz?',
            text: "Tamamla butonuna basmadan önce değişikliklerinizi Kaydet butonuna basarak kaydetttiğinizden emin olunuz. Yine de Tamamlansın mı?",
            icon: 'question',
            showCancelButton: true,
            confirmButtonText: 'Evet',
            cancelButtonText: 'Hayır',
            customClass: {
                confirmButton: 'btn btn-primary',
                cancelButton: 'btn btn-danger'
            }
        }).then((result) => {
            if (result.isConfirmed) {
                $.post("@Url.Action(nameof(KitaplarController.KitabiTamamla), "Kitaplar")",
                    {

                        "isWordPDF": false,
                        "isTamalama": true,
                         "BookModel":{
                            "ID": kitapId,
                            "Name":$("#kitapAdi").val(),
                            "onsoz":$("#onsoz").val(),
                            "ARKAKAPAKYAZISI":$("#arkaKapakYazi").val(),
                            "CategoriId":$("#kitapKetogri").val(),

                        },                    
                        "BookPagesModel":{
                            "PageWrite": sayfaYazi,
                            "ID":$("#BOOKPAGESID").val(),
                        },
                    },
                    function (data) {
                        console.log("data:" + data);
                        if (data.state == 1) {

                            BilgiVer(data.state, "#basarili-", "#bilgi-", "#uyari-", "Kitap Tamamlandı. İlgili kitaba yönlendiriliyorsunuz..");

                            setTimeout(function () {
                                var link = window.location.href;
                                //console.log("link " + link);
                                window.location.href = "/Kitaplar/KitapDetay?kitapId=" + kitapId.toString();

                            }, 3000);
                        } else {
                            BilgiVer(data.state, "#basarili-", "#bilgi-", "#uyari-", "Kitap Tamamlanamadı");
                        }


                    });
            } else if (result.dismiss === Swal.DismissReason.cancel) {
                Swal.fire('Kitap Tamamla', 'Kitap Tamamlama İşleminde sorun oluştu. Bizim ile iletişime geçiniz', 'error');
                $("#kitapKatdetmeUyariDurumu").val("0");
            }
        });
    });

    var kitapOlusturForm = document.getElementById('form-wrapper');
    kitapOlusturForm.addEventListener('submit', function (event) {
        event.preventDefault(); // Formun varsayılan submit işlemini engelle

        var kitapKatdetmeUyariDurumu = $("#kitapKatdetmeUyariDurumu").val();
        console.log("kitapKatdetmeUyariDurumu : " + kitapKatdetmeUyariDurumu);
        var kategoriBilgiId = $("#kitapKetogri").val();
        var selectlement = document.getElementById('selecData-' + kategoriBilgiId.toString());
        // data özelliğinden değeri al
        var kategoriAdi = selectlement.getAttribute('data');
        console.log("kategoriAdi : " + kategoriAdi);
        var metinText = "Seçtiğiniz Kategori (" + kategoriAdi + ") 'ye göre minimum sayfa sayısı 50 sayfa ekleme durumu vardır. Ve birkez kaydetme yaptıktan sonra ilgili kategoriyi değiştiremezsiniz. Yine de emin misiniz?";

        //isAlert(metinText);
        if (dataValue == 0 && kitapKatdetmeUyariDurumu == "0") {
            Swal.fire({
                title: 'Emin misiniz?',
                text: metinText,
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: 'Evet',
                cancelButtonText: 'Hayır',
                customClass: {
                    confirmButton: 'btn btn-primary',
                    cancelButton: 'btn btn-danger'
                }
            }).then((result) => {
                if (result.isConfirmed) {
                    var formData = new FormData(this); // FormData nesnesini oluştur
                    var dosyaInputs = kitapOlusturForm.querySelectorAll('input[type="file"]');
                    console.log(dosyaInputs.length);
                    for (var i = 0; i < dosyaInputs.length; i++) {
                        formData.append('dosyalar', dosyaInputs[i].files[0]);
                    }

                    veriGonder(formData);

                } else if (result.dismiss === Swal.DismissReason.cancel) {
                    // Hayır'a tıklandığında burası çalışır
                    Swal.fire('Kitap Ekleme', 'Ekleyeceğiniz kitabın kategorsini tekrardan belirleyebilirsiniz', 'error');
                    $("#kitapKatdetmeUyariDurumu").val("0");
                }
            });
        } else {
            farkliVeriGonder();
        }

    });

    function farkliVeriGonder() {
        var kitapId = $("#BOOKSID").val();
        var sayfaYazi = $("#sayfaYazi").val();
        $.post("@Url.Action(nameof(KitaplarController.KitapEkle), "Kitaplar")",
            {
                "BookModel":{
                    "ID": kitapId,
                    "Name":$("#kitapAdi").val(),
                    "onsoz":$("#onsoz").val(),
                    "ARKAKAPAKYAZISI":$("#arkaKapakYazi").val(),
                    "CategoriId":$("#kitapKetogri").val(),

                },
                "isWordPDF": false,
                "BookPagesModel":{
                    "PageWrite": sayfaYazi,
                    "ID":$("#BOOKPAGESID").val(),
                },
            },
            function (data) {
                console.log("data:" + data);
                if (data.state == 1) {
                    BilgiVer(data.state, "#basarili-", "#bilgi-", "#uyari-", (mesaj == "") ? "Kitaba Sayfa Eklenmiştir." : mesaj);

                    $("#BOOKSID").val(data.result.bookID);
                    $("#BOOKPAGESID").val(data.result.booksPageID);
                    console.log(" farkliVeriGonder : BOOKSID: " + $("#BOOKSID").val() + " BOOKPAGESID: " + $("#BOOKPAGESID").val());
                    $("#sayfaSayisi").html(data.result.booksPageCount + 1);

                    $("#sayfaYazi").html("");


                    $("#kitapKatdetmeUyariDurumu").val("1");
                    var intSayi = parseInt(dataValue);
                    spanElement.setAttribute('data', intSayi + 1);

                    prevBtn.classList.add('d-none');
                } else {
                    BilgiVer(data.state, "#basarili-", "#bilgi-", "#uyari-", response.message); //"Kitap Eklenemedi."
                }


            });

    }

    function veriGonder(formData) {

        var kitapId = $("#BOOKSID").val();
        console.log("kitapId:" + kitapId);
        var sayfaId = $("#BOOKPAGESID").val();
        console.log("sayfaId:" + sayfaId);
        var kitapAdi = $("#kitapAdi").val();
        var onsoz = $("#onsoz").val();
        var arkaKapakYazi = $("#arkaKapakYazi").val();
        var kategori = $("#kitapKetogri").val();
        var sayfaYazi = $("#sayfaYazi").val();

        formData.append('BookModel.ID', kitapId);
        formData.append('BookModel.Name', kitapAdi);
        formData.append('BookModel.onsoz', onsoz);
        formData.append('BookModel.ARKAKAPAKYAZISI', arkaKapakYazi);
        formData.append('BookModel.CategoriId', kategori);
        formData.append('isWordPDF', false);
        formData.append('BookPagesModel.PageWrite', sayfaYazi);
        formData.append('BookPagesModel.ID', sayfaId);


        $.ajax({
            url: 'KitapEkle', // Formun gönderileceği adresi belirtin
            method: 'POST',
            data: formData, // Form verilerini FormData nesnesiyle al
            processData: false,
            contentType: false,
            success: function (response) {
                console.log('Başarılı dönüş: response', response);
                console.log('Başarılı dönüş: response result ', response.result);
                // Başarılı dönüş sonucunu işleme kodları buraya gelebilir.
                if (response.state == 1) {

                    BilgiVer(response.state, "#basarili-", "#bilgi-", "#uyari-", "Kitap ve Sayfası Eklenmiştir.");

                    $("#BOOKSID").val(response.result.bookID);
                    $("#BOOKPAGESID").val(response.result.booksPageID);
                    console.log("veriGonder BOOKSID: " + $("#BOOKSID").val() + " BOOKPAGESID: " + $("#BOOKPAGESID").val());
                    $("#sayfaSayisi").html(response.result.booksPageCount + 1);

                    $("#kitapKatdetmeUyariDurumu").val("1");
                    var intSayi = parseInt(dataValue);
                    spanElement.setAttribute('data', intSayi + 1);

                    prevBtn.classList.add('d-none');

                } else {
                    BilgiVer(response.state, "#basarili-", "#bilgi-", "#uyari-", response.message);//"Kitap ve Sayfası Eklenemedi."
                }
            },
            error: function (xhr, status, error) {
                console.error('Hata:', error);
                $("#kitapKatdetmeUyariDurumu").val("0");
                // Hata durumunu işleme kodları buraya gelebilir
            }
        });
    }

    function BilgiVer(state, basarili, bilgi, uyari, mesaj) {
        if (state == 1) {
            $(basarili + tip).css("display", "block");
            $(basarili + tip + " #mesaj").html(mesaj);

            $(bilgi + tip).css("display", "none");
            $(uyari + tip).css("display", "none");

            /*setTimeout(function () {
                var link = window.location.href;

                window.location.href = link;
                console.log("link " + link);

                window.location.reload()

            }, 3000);*/
        }
        else if (state == 3) {
            $(bilgi + tip).css("display", "block");
            $(bilgi + tip + " #mesaj").html(mesaj);

            $(basarili + tip).css("display", "none");
            $(uyari + tip).css("display", "none");
        }
        else {
            $(uyari + tip).css("display", "block");
            $(bilgi + tip).css("display", "none");
            $(basarili + tip).css("display", "none");
            $(uyari + tip + " #mesaj").html(mesaj);
        }
    }

    form.onsubmit = () => {
        return false
    }

    var current_step = 0;
    var stepCount = step.length;



    if (dataValue != "0") {
        step[current_step].classList.add('d-none');
        step[current_step].style.display = "none";

        step[current_step + 1].classList.add('d-block');
        step[current_step + 1].style.display = "block";

        current_step = current_step + 1;

        nextBtn.classList.add('d-none');
        prevBtn.classList.add('d-none');

        tamamBtn.classList.add('d-inline-block');
        submitBtn.classList.add('d-inline-block');
        progress(100);
    } else {
        step[current_step].classList.add('d-block');
        step[current_step].style.display = "block";
    }

    /* step[current_step].classList.add('d-block');
     step[current_step].style.display = "block";*/

    if (current_step == 0) {
        prevBtn.classList.add('d-none');
        tamamBtn.classList.add('d-none');
        submitBtn.classList.add('d-none');
        nextBtn.classList.add('d-inline-block');
    }

    var previous_step = 0;

    nextBtn.addEventListener('click', () => {
        current_step++;
        console.log("nextBtn current_step:" + current_step);
        var previous_step = current_step - 1;
        console.log("previous_step:" + previous_step);

        if ((current_step > 0) && (current_step < stepCount)) {

            prevBtn.classList.remove('d-none');
            prevBtn.classList.add('d-inline-block');


            step[current_step].classList.remove('d-none');
            step[current_step].style.display = "none";

            step[current_step].classList.add('d-block');
            step[current_step].style.display = "block";

            step[previous_step].classList.remove('d-block');
            step[previous_step].style.display = "block";

            step[previous_step].classList.add('d-none');
            step[previous_step].style.display = "none";

            if (current_step == stepCount - 1) { // 3 tane eleman var ise 3. index olmaz son eleman 2. indextir

                submitBtn.classList.remove('d-none');
                submitBtn.classList.add('d-inline-block');

                tamamBtn.classList.remove('d-none');
                tamamBtn.classList.add('d-inline-block');

                nextBtn.classList.remove('d-inline-block');
                nextBtn.classList.add('d-none');
            }

        } else {

            if (current_step > stepCount) {
                form.onsubmit = () => {
                    return true
                }
            }
        }
        progress((100 / stepCount) * current_step);
    });


    prevBtn.addEventListener('click', () => {
        if (current_step > 0) {
            console.log("prevBtn current_step:" + current_step);
            current_step--;
            let previous_step = current_step + 1;


            console.log("previous_step:" + previous_step);

            prevBtn.classList.add('d-none');
            prevBtn.classList.add('d-inline-block');


            step[current_step].classList.remove('d-none');
            step[current_step].style.display = "none";

            step[current_step].classList.add('d-block')
            step[current_step].style.display = "block";
            if (previous_step < stepCount) {
                step[previous_step].classList.remove('d-block');
                step[previous_step].style.display = "block";

                step[previous_step].classList.add('d-none');
                step[previous_step].style.display = "none";
            }


            if (current_step < stepCount) {
                submitBtn.classList.remove('d-inline-block');
                submitBtn.classList.add('d-none');

                tamamBtn.classList.remove('d-inline-block');
                tamamBtn.classList.add('d-none');

                nextBtn.classList.remove('d-none');
                nextBtn.classList.add('d-inline-block');

                prevBtn.classList.remove('d-none');
                prevBtn.classList.add('d-inline-block');
            }
        }

        if (current_step == 0) {
            prevBtn.classList.remove('d-inline-block');
            prevBtn.classList.add('d-none');
        }
        progress((100 / stepCount) * current_step);
    });


</script>

