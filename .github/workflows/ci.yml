name: CI

on:
  push:
    branches:
      - main
      - test
      - development
  pull_request:
    branches:
      - main
      - test

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read   # 👈 ÖNEMLİ (GitHub Packages için)

    steps:

      # --------------------
      # Checkout Repo
      # --------------------
      - name: Checkout UI
        uses: actions/checkout@v4

      # --------------------
      # Setup .NET
      # --------------------
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # --------------------
      # GitHub Packages Auth
      # --------------------
      - name: Add GitHub Packages source
        run: |
          dotnet nuget add source \
            --username mansur61 \
            --password ${{ secrets.GITHUB_TOKEN }} \
            --store-password-in-clear-text \
            --name github \
            "https://nuget.pkg.github.com/mansur61/index.json"

      # --------------------
      # Restore / Build / Test
      # --------------------
      - run: dotnet restore HerkesYazarOlsun.Portal/HerkesYazarOlsun.Portal.csproj
      - run: dotnet build HerkesYazarOlsun.Portal/HerkesYazarOlsun.Portal.csproj -c Release --no-restore
      - name: Run Tests (only for test/main)
        if: github.ref_name == 'test' || github.ref_name == 'main'
        run: dotnet test HerkesYazarOlsun.Portal/HerkesYazarOlsun.Portal.csproj -c Release --no-build

      # --------------------
      # Publish (Sadece test ve main)
      # --------------------
      - name: Publish
        if: github.ref_name == 'test' || github.ref_name == 'main'
        run: dotnet publish HerkesYazarOlsun.Portal/HerkesYazarOlsun.Portal.csproj -c Release -o publish

      # --------------------
      # TEST DEPLOY
      # --------------------
      - name: Deploy TEST
        if: github.ref_name == 'test'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: publish/
          server-dir: /test.herkesyazarolsun.com.tr/test/

      - name: Health Check TEST
        if: github.ref_name == 'test'
        run: curl -f https://test.herkesyazarolsun.com.tr/health

      # --------------------
      # PROD DEPLOY
      # --------------------
      - name: Deploy PROD
        if: github.ref_name == 'main'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: publish/
          server-dir: https://herkesyazarolsun.com.tr/httpdocs/prod/

      - name: Health Check PROD
        if: github.ref_name == 'main'
        run: curl -f https://herkesyazarolsun.com.tr/health
